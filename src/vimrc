" Legacy Vim compatibility wrapper
" This file provides compatibility for tools that expect ~/.vimrc
" It loads the modern Lua configuration when running in Neovim

if has('nvim')
    " Running in Neovim - load the modern Lua config
    lua dofile(vim.fn.stdpath('config') .. '/init.lua')
else
    " Running in legacy Vim - provide basic fallback
    echo "This configuration is optimized for Neovim. Please install Neovim for the full experience."
    " Basic fallback settings for legacy Vim
    set number
    set relativenumber
    set expandtab
    set shiftwidth=2
    set tabstop=2
    syntax on
    set hlsearch
    set incsearch
endif