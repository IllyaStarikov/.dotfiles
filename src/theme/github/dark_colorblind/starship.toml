# GitHub Dark Colorblind - Starship Configuration
# Generated from template - do not edit directly
# Full prompt config merged from the legacy src/zsh/starship.toml (2026-07):
# the theme switcher owns ~/.config/starship.toml, so the rich prompt lives
# here and every theme variant carries it.

# Prompt format - clean and informative
format = """
$username\
$hostname\
$directory\
$git_branch\
$git_commit\
$git_state\
$git_status\
$git_metrics\
$python\
$nodejs\
$rust\
$golang\
$docker_context\
$kubernetes\
$fill\
$cmd_duration\
$time\
$line_break\
$jobs\
$battery\
$status\
$character"""

# Right-side prompt (time and duration moved here for cleaner left prompt)
right_format = ""

# Continuation prompt for multi-line commands
continuation_prompt = "[∙](bright-black) "

# Global timeout for external commands (ms) - 1s is sufficient
command_timeout = 1000

# Prompt character configuration
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vicmd_symbol = "[❮](bold green)"

# Directory configuration (style adapts to the theme's light/dark mode)
[directory]
truncation_length = 3
truncate_to_repo = true
format = "[$path]($style) "
style = "cyan bold"
read_only = " 🔒"
truncation_symbol = "…/"
home_symbol = "~"

# Show when in specific important directories
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"Projects" = "󰲋 "

# Git configuration - essential info only
[git_branch]
format = "on [$symbol$branch(:$remote_branch)]($style) "
symbol = "⎇ "
style = "bold purple"
truncation_length = 20
truncation_symbol = "…"

[git_status]
format = '([$all_status$ahead_behind]($style) )'
style = "bold red"
conflicted = "="
ahead = "⇡"
behind = "⇣"
diverged = "⇕"
up_to_date = ""
untracked = "?"
stashed = '📦${count}'
modified = "!"
staged = "+"
renamed = "»"
deleted = "✘"

# Git commit - show hash when in detached HEAD state
[git_commit]
tag_disabled = false
format = '[\($hash$tag\)]($style) '
style = "bold yellow"

# Git state - show rebase/merge/cherry-pick status
[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bold yellow"

# Git metrics - show added/deleted lines
[git_metrics]
disabled = false
added_style = "bold green"
deleted_style = "bold red"
only_nonzero_diffs = true
format = '([+$added]($added_style) )([-$deleted]($deleted_style) )'

# Show command duration for slow commands
[cmd_duration]
min_time = 1000
format = "took [$duration]($style) "
style = "yellow bold"

# Username - only show when not default user or in SSH
[username]
style_user = "green bold"
style_root = "red bold"
format = "[$user]($style) "
disabled = false
show_always = false

# Hostname - only show in SSH sessions
[hostname]
ssh_only = true
format = "[@$hostname]($style) "
style = "green dimmed"
trim_at = "."

# Language/Framework modules - show only when relevant
[python]
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
symbol = "🐍 "
style = "yellow bold"
pyenv_version_name = false
pyenv_prefix = ""
python_binary = ["python3", "python"]
detect_extensions = ["py"]
detect_files = ["requirements.txt", "pyproject.toml", "Pipfile", "poetry.lock"]
detect_folders = ["__pycache__", ".venv", "venv"]

[nodejs]
format = "via [$symbol($version )]($style)"
symbol = "⬢ "
style = "green bold"
detect_extensions = ["js", "mjs", "cjs", "ts", "tsx", "jsx"]
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]
version_format = "v${major}.${minor}"

[rust]
format = "via [$symbol($version )]($style)"
symbol = "🦀 "
style = "red bold"
detect_extensions = ["rs"]
detect_files = ["Cargo.toml", "Cargo.lock"]
detect_folders = []
version_format = "v${major}.${minor}"

[golang]
format = "via [$symbol($version )]($style)"
symbol = "🐹 "
style = "cyan bold"
detect_extensions = ["go"]
detect_files = ["go.mod", "go.sum", "go.work"]
detect_folders = []
version_format = "v${major}.${minor}"

# Docker - show only when in use
[docker_context]
format = "via [$symbol$context]($style) "
symbol = "🐳 "
style = "blue bold"
only_with_files = true
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile"]
detect_folders = []

# Kubernetes - show context with production warning
[kubernetes]
format = 'on [$symbol$context( \($namespace\))]($style) '
symbol = "☸ "
style = "cyan bold"
disabled = false
detect_files = ["k8s", "Kubernetes"]
detect_folders = ["k8s", ".kube"]
detect_extensions = []

# Production context warning - red for safety
[[kubernetes.contexts]]
context_pattern = ".*prod.*"
style = "bold red"

[[kubernetes.contexts]]
context_pattern = ".*production.*"
style = "bold red"

# Fill module - pushes right content to edge
[fill]
symbol = " "

# Jobs indicator
[jobs]
symbol = "+"
style = "bold blue"
number_threshold = 1
symbol_threshold = 1
format = "[$symbol$number]($style) "

# Battery - only show when low
[battery]
full_symbol = ""
charging_symbol = "⚡️"
discharging_symbol = "💀"
unknown_symbol = "❓"
empty_symbol = "❗️"
format = "[$symbol$percentage]($style) "
disabled = false

[[battery.display]]
threshold = 20
style = "bold red"

[[battery.display]]
threshold = 30
style = "bold yellow"

# Time - optional, disabled by default
[time]
disabled = true
format = '[$time]($style) '
style = "yellow"
use_12hr = false
time_format = "%H:%M"

# Exit status - show on error
[status]
style = "bg:red"
symbol = "✖"
success_symbol = ""
format = '[$symbol $common_meaning$signal_name$maybe_int]($style) '
map_symbol = true
disabled = false
pipestatus = true

# Performance optimizations - disable unnecessary modules
[aws]
disabled = true

[gcloud]
disabled = true

[package]
disabled = true

[helm]
disabled = true

[terraform]
disabled = true

[buf]
disabled = true

[cmake]
disabled = true

[dart]
disabled = true

[dotnet]
disabled = true

[elixir]
disabled = true

[elm]
disabled = true

[erlang]
disabled = true

[haskell]
disabled = true

[java]
disabled = true

[julia]
disabled = true

[kotlin]
disabled = true

[lua]
disabled = true

[nim]
disabled = true

[nix_shell]
disabled = true

[ocaml]
disabled = true

[perl]
disabled = true

[php]
disabled = true

[purescript]
disabled = true

[rlang]
disabled = true

[ruby]
disabled = true

[scala]
disabled = true

[swift]
disabled = true

[vagrant]
disabled = true

[zig]
disabled = true

[conda]
disabled = true

[spack]
disabled = true

[memory_usage]
disabled = true

[openstack]
disabled = true

[crystal]
disabled = true

[vlang]
disabled = true
