# AI-Powered Coding Showcase 🤖
# Demonstrate the future of coding with integrated AI assistance

name: ai_coding_showcase
root: ~/

# Launch with: tmuxinator start ai_coding_showcase

windows:
  - ai_chat:
      layout: even-horizontal
      panes:
        - nvim -c "echo '🤖 AI-Powered Development Environment'" -c "echo ''" -c "echo 'Press <leader>cc to open AI chat'" -c "echo 'Press <leader>ca for action palette'" -c "echo ''" -c "echo 'Available AI Adapters:'" -c "echo '  • Local Ollama (default)'" -c "echo '  • Anthropic Claude'" -c "echo '  • OpenAI GPT'" -c "echo '  • GitHub Copilot'" -c "echo ''" -c "echo 'Try: Select code and press <leader>cr for instant code review!'"
        - |
          echo "📊 Live AI Status Monitor"
          echo "========================"
          echo ""
          echo "Current Model: Ollama llama3.2:latest"
          echo "Status: Ready"
          echo ""
          echo "Quick Actions:"
          echo "  • <leader>co - Optimize code"
          echo "  • <leader>ct - Generate tests" 
          echo "  • <leader>ce - Explain code"
          echo "  • <leader>cf - Fix bugs"
          echo ""
          watch -n 1 'echo "AI Sessions: $(ps aux | grep -c ollama)"'

  - code_evolution:
      layout: main-vertical
      main-pane-width: 60%
      panes:
        - |
          cat << 'EOF' > /tmp/demo_code.py
          # Messy code that needs AI help
          def calculate_stuff(x,y,z):
              result=x+y
              if z>0:
                  for i in range(z):
                      result=result*2
              return result
          
          # TODO: This needs optimization
          def find_item(lst,item):
              for i in range(len(lst)):
                  if lst[i]==item:
                      return i
              return -1
          EOF
          nvim /tmp/demo_code.py -c "echo '🔧 Select code and use AI to transform it!'" -c "echo 'Try: Visual select a function, then <leader>co to optimize'"
        - |
          echo "🎯 AI Transformation Commands"
          echo "============================="
          echo ""
          echo "1. Select the messy function"
          echo "2. Press <leader>co to optimize"
          echo "3. Watch AI refactor in real-time"
          echo ""
          echo "Other transformations:"
          echo "  • <leader>cm - Add documentation"
          echo "  • <leader>ct - Generate unit tests"
          echo "  • Convert to different languages"
          echo ""
          echo "💡 Pro tip: Use slash commands in chat!"
          echo "   /file - Insert current file"
          echo "   /buffer - Insert buffer content"
          echo "   /symbols - Insert code symbols"
          
  - prompt_library:
      layout: tiled
      panes:
        - nvim -c "CodeCompanion" -c "echo 'Explore the AI prompt library with <leader>ca'"
        - |
          echo "📚 Custom AI Prompts Available"
          echo "=============================="
          echo ""
          echo "• Code Review (Production-grade)"
          echo "• Performance Optimization"
          echo "• Security Audit"
          echo "• API Documentation Generator"
          echo "• Design Pattern Suggestions"
          echo "• Refactoring Assistant"
          echo "• Test Coverage Analysis"
          echo ""
          echo "💬 Chat with multiple AI models!"
          echo "Switch between Ollama/Claude/GPT/Copilot"