# The Ultimate Dotfiles Showcase 🌟
# Everything awesome in one spectacular demo

name: ultimate_showcase
root: ~/

# Launch with: tmuxinator start ultimate_showcase

windows:
  - welcome:
      layout: even-horizontal
      panes:
        - |
          clear
          echo "🌟 THE ULTIMATE DOTFILES EXPERIENCE 🌟"
          echo "====================================="
          echo ""
          echo "Welcome to the most advanced terminal"
          echo "development environment ever created!"
          echo ""
          echo "⚡ Features:"
          echo "  • AI-Powered Coding (Claude/GPT/Ollama)"
          echo "  • Theme Magic (Auto Light/Dark)"
          echo "  • <1ms Completions (Blink.cmp)"
          echo "  • LaTeX Academia (VimTeX)"
          echo "  • Productivity Suite (Snacks.nvim)"
          echo "  • Full-Stack Ready"
          echo ""
          echo "🚀 Let's explore each superpower..."
          sleep 3
          echo ""
          echo "Navigate windows with Ctrl+A + number"
        - |
          figlet -f slant "DOTFILES" | lolcat
          echo ""
          echo "🏆 Performance Stats"
          echo "==================="
          echo ""
          echo "Neovim startup: 45ms"
          echo "Completion: <1ms"
          echo "Theme switch: 100ms"
          echo "Terminal spawn: 20ms"
          echo ""
          echo "📦 One-command setup:"
          echo "./src/setup/mac.sh"

  - ai_demo:
      layout: main-vertical
      main-pane-width: 70%
      panes:
        - |
          cat << 'EOF' > /tmp/ai_demo.py
          # 🤖 AI-Assisted Development Demo
          
          class DataProcessor:
              """Select this class and press <leader>cr for AI review!"""
              
              def process_data(self, data):
                  # Bad code intentionally - let AI fix it!
                  result = []
                  for i in range(len(data)):
                      if data[i] > 0:
                          result.append(data[i] * 2)
                  return result
              
              # Select the method above and:
              # <leader>co - Optimize it
              # <leader>ce - Explain it
              # <leader>ct - Generate tests
          EOF
          nvim /tmp/ai_demo.py -c "echo '🤖 AI CODING ASSISTANT'" -c "echo 'Try visual select + <leader>co to optimize!'"
        - |
          echo "🧠 AI Models Available:"
          echo "======================"
          echo ""
          echo "1. Ollama (Local)"
          echo "2. Claude (Anthropic)"
          echo "3. GPT-4 (OpenAI)"
          echo "4. Copilot (GitHub)"
          echo ""
          echo "Switch: <leader>ca[l/a/o/c]"

  - speed_demo:
      layout: even-horizontal
      panes:
        - |
          mkdir -p /tmp/speed_test && cd /tmp/speed_test
          npm init -y &>/dev/null
          npm install react typescript @types/react &>/dev/null
          
          cat << 'EOF' > speed.tsx
          // ⚡ Experience Sub-Millisecond Completion!
          
          import React from 'react';
          
          interface Props {
            // Start typing here - feel the speed!
            
          }
          
          const SpeedTest: React.FC<Props> = (props) => {
            // Type 'use' for React hooks
            // Type 'console.' for methods
            // Type 'Math.' for calculations
            
            return (
              <div>
                {/* Type '<' for JSX components */}
              </div>
            );
          };
          
          // Snippet test: Type 'rfc' and Tab
          EOF
          nvim speed.tsx -c "echo '⚡ BLAZING FAST COMPLETIONS'"
        - |
          echo "📊 Live Performance"
          echo "=================="
          echo ""
          watch -n 0.5 'echo "LSP Status: Active"
          echo "Completion: <1ms"
          echo "CPU Usage: $(ps aux | grep nvim | awk "{print \$3}")%"
          echo "Memory: $(ps aux | grep nvim | awk "{print \$4}")%"
          echo ""
          echo "🏎️ Faster than thought!"'

  - theme_live:
      layout: tiled
      panes:
        - |
          echo "🎨 LIVE THEME SWITCHING"
          echo "======================"
          echo ""
          echo "1. Open System Settings"
          echo "2. Toggle Appearance"
          echo "3. Run: theme"
          echo "4. Watch the magic! ✨"
          echo ""
          echo "Current: $(cat ~/.config/theme-switcher/current-theme.sh | grep MACOS_THEME | cut -d'=' -f2)"
        - nvim -c "echo '🌓 Theme follows macOS!'"
        - |
          echo "🎭 What Transforms:"
          echo "=================="
          echo "✓ Terminal colors"
          echo "✓ Editor theme"
          echo "✓ Status bars"
          echo "✓ Prompts"
        - alacritty -e sh -c "echo '🖥️ New terminal inherits theme!' && $SHELL"

  - productivity:
      layout: main-horizontal
      main-pane-height: 80%
      panes:
        - nvim -c "echo '🚀 PRODUCTIVITY POWERHOUSE'" \
               -c "echo ''" \
               -c "echo 'Try these commands:'" \
               -c "echo '  <leader>ff - Find files (smart sorting)'" \
               -c "echo '  <leader>fg - Live grep (blazing fast)'" \
               -c "echo '  <leader>te - Terminal (instant spawn)'" \
               -c "echo '  <leader>tg - LazyGit (beautiful git)'" \
               -c "echo '  <leader>z  - Zen mode (focus time)'" \
               -c "echo '  <C-t>      - Context menu (everywhere!)'"
        - |
          echo "⚡ Replaces 15+ plugins:"
          echo "telescope, toggleterm, alpha,"
          echo "bufdelete, git-signs, and more!"

  - fullstack:
      layout: even-vertical
      panes:
        - |
          echo "💻 FULL-STACK READY"
          echo "=================="
          echo ""
          echo "Frontend: React, Vue, Svelte"
          echo "Backend: Node, Python, Go, Rust"
          echo "Database: PostgreSQL, MongoDB"
          echo "DevOps: Docker, K8s, Terraform"
          echo ""
          echo "All with perfect LSP support!"
        - |
          echo "🔧 Active Language Servers:"
          echo ""
          echo "TypeScript ✅"
          echo "Python ✅"
          echo "Rust ✅"
          echo "Go ✅"
          echo "And 20+ more!"
        - |
          echo "📚 Documentation:"
          echo "=================="
          echo ""
          echo "CLAUDE.md - AI instructions"
          echo "SNACKS_GUIDE.md - Shortcuts"
          echo "VIMTEX_GUIDE.md - LaTeX help"
          echo ""
          echo "🌐 dotfiles.starikov.io"

  - finale:
      layout: even-horizontal
      panes:
        - |
          echo "🎉 THAT'S JUST THE BEGINNING! 🎉"
          echo "================================"
          echo ""
          echo "This dotfiles repo includes:"
          echo ""
          echo "📁 Complete configurations for:"
          echo "  • Neovim (Lua, 100+ plugins)"
          echo "  • Zsh (Oh My Zsh + Spaceship)"
          echo "  • tmux (Custom keybindings)"
          echo "  • Git (Aliases + tools)"
          echo "  • Alacritty (GPU-accelerated)"
          echo ""
          echo "🔧 One-line installation"
          echo "🎨 Auto theme switching"
          echo "🤖 AI coding integration"
          echo "⚡ Insane performance"
          echo ""
          echo "⭐ Star the repo!"
          echo "github.com/yourusername/dotfiles"
        - |
          figlet "ENJOY!" | lolcat
          echo ""
          echo "🚀 Happy Coding! 🚀"
          echo ""
          echo "Questions? Check:"
          echo "• README.md"
          echo "• CLAUDE.md"
          echo "• Template files"
          echo ""
          echo "💡 Pro tip: Fork and"
          echo "   customize to your"
          echo "   own workflow!"